{
  "name": "paragon-supplier-hub",
  "private": true,
  "version": "1.4.0",
  "type": "module",
  "description": "Paragon Supply Collaboration Hub — portal pemasok, konsol procurement, dan backend Node + SQLite",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node scripts/check.mjs",
    "server": "npm --prefix server start",
    "server:dev": "npm --prefix server run dev",
    "server:install": "npm --prefix server install",
    "db:migrate": "npm --prefix server run migrate",
    "db:seed": "npm --prefix server run seed",
    "db:reset": "npm --prefix server run reset",
    "server:test": "npm --prefix server test"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.26.2"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "react-test-renderer": "^18.3.1",
    "vite": "^5.4.8"
  }
}
